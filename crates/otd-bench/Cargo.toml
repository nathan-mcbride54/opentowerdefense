[package]
name = "otd-bench"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Headless Open Tower Defense runner for map validation and replays"

[[bin]]
name = "otd-bench"
path = "src/main.rs"

[dependencies]
otd-core = { path = "../otd-core" }
serde_json = "1"
