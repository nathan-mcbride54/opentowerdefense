{
  "name": "open-tower-defense",
  "private": true,
  "description": "Open-source maze tower defense. Shape the kill zone. Hold the relay.",
  "license": "MIT",
  "scripts": {
    "wasm": "wasm-pack build crates/otd-wasm --dev --target web --out-dir ../../web/src/lib/wasm --out-name otd",
    "wasm:release": "wasm-pack build crates/otd-wasm --release --target web --out-dir ../../web/src/lib/wasm --out-name otd",
    "dev": "npm run wasm && npm --prefix web run dev",
    "dev:fast": "npm run wasm:release && npm --prefix web run dev",
    "build": "npm run wasm:release && npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "check": "npm run wasm && npm --prefix web run check",
    "test": "cargo test -p otd-core",
    "bench": "cargo run -p otd-bench --"
  }
}
