# Cloudflare static-asset headers (picked up by wrangler deploy).
# SvelteKit content-hashes everything under _app/immutable, so it can be cached forever.
/_app/immutable/*
  Cache-Control: public, max-age=31536000, immutable
